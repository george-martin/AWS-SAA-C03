# AWS-SAA-C03
SAA-C03 preparations

You can set below secrets within Github's Settings → Secrets and variables → Codespaces → New repository secret.

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=
